## v1.3.2

### 独立窗口发送

- Bridge 基础配置新增“独立窗口发送”开关，默认关闭。开启后直接定位并激活已打开的目标聊天窗口，跳过联系人搜索，支持文字、图片和文件。
- 仅白名单模式允许开启；开启后禁止切回黑名单，界面、配置保存和运行时均增加校验。私聊、群聊和本地指令回复统一受白名单限制，空白名单不发送。
- 开启后持续提醒用户：手动将白名单中选择的每个会话打开为独立聊天窗口，并保持窗口存在，可最小化。
- 每次发送重新匹配窗口；未找到、存在同名候选、标题或进程变化、前台焦点丢失时停止发送，不回退搜索。
- 切换发送模式并保存时，自动重启正在运行的 Bridge；关闭后恢复原有搜索发送方式。

### 会话过滤与名称显示

- 修复消息过滤列表只扫描群聊、无法直接选择私聊的问题。“选择群聊”改为“选择会话”，同时展示群聊和私聊/其他会话，并标注类型。
- 支持按名称或真实会话 ID 搜索，勾选结果保存真实会话 ID，重新加载后保留选择。
- 扫描后补全联系人备注或昵称；补全失败或超时时仍保留 ID，不隐藏会话。
- 移除用最近消息摘要代替会话名称的回退，避免列表大量显示为 `[消息]`。
- 修复消息推送中原始会话 ID 抢占已解析昵称的问题，回复路由优先采用可用的联系人显示名称。

### 使用提示

- 独立窗口发送仍会将微信置于前台，不是后台静默发送；请保持聊天输入框可输入，避免发送时同时操作微信。
- 窗口标题不是永久会话 ID，无法仅凭名称证明聊天对象身份；请避免同名会话。原有搜索发送模式仍只过滤群聊，不改变其私聊权限范围。

## v1.3.1 - 2026-09-21

- 更新 AstrWeChat 品牌与 Bridge 启动流程，提升连接稳定性。
- 新增 AW 本地命令配置与管理功能。
- 修复新版微信窗口识别、激活和联系人切换问题。
- 设置页新增 UIA 详细日志开关，默认关闭，支持运行时生效。

## v1.3.0 - 已发布

### 连接分组与切换

- 新增 AstrBot / KouriChat 独立连接分组，分别保存地址与 Token，采用单实例互斥切换。
- 保存前校验目标配置；当前连接参数变化时先等待旧 Bridge 退出，再启动新连接。未启用组修改不打断当前连接。
- 增加服务端名称、连接错误与鉴权提示，完善进程退出隔离、状态输出分片解析及配置持久化。
- 完成本地 WS 与 KouriChat 适配器级联调。

### 微信连接与启动

- Bridge 自动沿用已连接的微信身份，启动时自动配置本地 HTTP / SSE 消息推送与 Token，无需重复填写微信来源地址和 Bot 微信 ID。
- 移除独立 API 配置页面，后台接口与媒体能力保留，由 Bridge 启动流程管理。
- 切换或断开微信数据库前先停止旧 Bridge，避免继续使用旧账号身份发送消息。
- 增加启动阶段进度、超时和失败提示，避免启动过程无反馈；统一微信数据库连接成功提示。

### 主动回复与 Work 面板

- 新增群聊主动回复上下文，可配置携带最近消息的行数，支持 0–100 行。
- 修复切换页面返回 Bridge 时重复播放历史 Work 工作流通知的问题，只展示初始化之后的新步骤。

### 配置保存与恢复

- 桌面启动的 Bridge 将配置和运行数据放入可写的用户目录，兼容迁移旧配置，避免依赖安装目录写权限。
- 配置采用临时文件替换并保留有效备份；遇到损坏时保留原文件，尝试恢复备份并显示提示。
- 增加配置读取状态、超时、错误提示和重试入口，避免读取失败后用默认值覆盖已有配置。

### 统计与消息库诊断

- 新增最近 30 天快速统计，优化统计范围与缓存管理。
- 消息数据库未识别或消息总数读取失败时明确报错，不再将异常当作零消息缓存；更新统计缓存版本，避免继续使用旧结果。
- 新增消息库文件清单、受限表结构探测、诊断采集脚本和健康检查测试，区分会话库连接成功与消息库实际可用。
- 新消息读取失败时输出警告，便于排查推送无消息的问题；相关诊断不代表已经修复底层消息库识别故障。

### 版本初始化

- 2026-09-11：以当前 1.2.0 工作区为基线建立独立的 `1.3.0/` 源码目录，保留原版本。
- 同步应用与锁文件项目版本号，继承现有构建与安装包检查流程。
- 初始化时确定 AstrBot、KouriChat 独立连接配置分组方向，现已实现互斥切换，不同时运行。
- 初始化阶段不包含功能修改；本版本后续完成的功能与修复见上方。

## v1.2.0 - 后续维护，未发布

以下为 `1.2.0/` 工作区的后续修改，不代表已替换 2026-09-05 发布的安装包。

### 统计与消息库诊断

- 同步消息库可用性检查、诊断采集脚本及新消息读取失败日志。
- 消息总数或每日趋势读取失败时显示错误，不再将失败结果显示为零；更新统计缓存版本。

### 安装包精简与检查

- 调整前端依赖为构建期依赖，移除精简版未使用的遗留依赖，减少重复打包。
- 精简 Windows 语言资源，排除源码映射、未使用字体，以及 Bridge 本机配置、日志和缓存；保留关键原生库与媒体处理资源。
- 新增打包规则测试与 Windows 产物检查，缺少关键资源或混入不应打包的文件时中止构建。
- 构建脚本显示同名安装包的新旧体积及减少比例，并补充打包使用说明。

## v1.2.0 - 2026-09-05

Bridge 概览升级为日志驱动的 Work 面板，并完善私聊定位、会话权限与安装构建流程。

### 概览与工作流

- 概览调整为“运行状态、Work、回复策略、会话过滤”布局，默认打开概览标签。
- 新增 Work 面板，根据实际 Bridge 日志展示接收微信消息、推送 AstrBot、发送队列、UIA 发送及文件、图片发送步骤。
- 工作流条目自动弹入，停留后收拢弹走；新步骤追加到上一条下方，不替换已有步骤。
- Work 面板保持与运行状态卡片等高，消息过多时在面板内部滚动，避免页面跳动。
- 群聊回复文案由“需要 @”调整为“响应@”，并移除概览中的连接配置摘要。

### Bridge 配置与路由

- 基础配置新增“以微信ID查找联系人”开关。
- 开启后，私聊回复优先使用微信 ID 定位会话，降低昵称重名导致发错联系人的风险。
- 增加私聊会话 ID 与群聊会话 ID 的独立映射，配置保存后支持热更新。
- 快速停止、启动 Bridge 时清理旧私聊路由，避免残留映射影响后续回复。

### 会话权限

- 新增全局会话白名单/黑名单，控制 @、命令、图片、文件、批量消息及异步回复。
- 支持自动扫描微信群聊、搜索多选、手动输入会话 ID，并保留尚未扫描到的配置。
- 兼容 v1.1.x 的 `active_reply_whitelist`，保存时提示迁移后的权限变化。
- 队列中的回复发送前会重新校验最新过滤规则。

### 构建与安装

- 新增 `npm run build-installer` 命令，同时保留 `build-installer.bat`。
- 修复 Windows PowerShell 5.1 读取 UTF-8 `package.json` 乱码导致构建失败的问题。
- Electron 43 固定为精确版本，并在首次需要时自动下载运行时。
- 构建脚本支持复用本机 Electron 和 ffmpeg 二进制，减少 GitHub 下载超时的影响。
- 修复依赖目录不完整时误跳过安装的问题。

## v1.1.1 - 2026-09-03

优化数据库重连、Bridge 稳定性与统计页面，并补充 AstrBot 反向 WebSocket Token 鉴权。

9.3
- 新增连接页面“重启微信连接”，保存配置后可直接重启应用并重新连接。
- 新增 AstrBot 反向 WebSocket Token 配置，支持通过 `Authorization: Bearer <Token>` 进行鉴权。
- 修复切换数据库时 WCDB Worker 未重建导致连接失败的问题。
- 修复 Bridge 快速停止、启动时可能产生重复 WebSocket 客户端和消息路由失效的问题。
- 修复私聊消息被主动回复概率误过滤的问题，主动回复概率现在仅作用于未明确 @Bot 的群聊普通消息。
- 新增 AstrBot 视频消息转发支持。
- 优化统计页缓存、加载进度与运行时间展示，减少重复扫描。
- 优化数据库连接状态保持，切换页面后不再重复打开数据库。

## v1.1.0 - 2026-08-27

添加主动回复，更新 Bridge 与安装器，优化统计页面与缓存逻辑。

8.27
- 修复私聊时 Bridge 会根据主动回复设定的概率推送消息至 AstrBot。
